# CLAUDE.md — GoldenKingBug Spawn Map

Source of truth for the **"GoldenKingBug Spawn Map"** Artifact — an interactive 3D (Three.js) boss-spawn
and warp-point map of Needle Cave.

- `GoldenKingBug-spawn-map.html` is the full artifact source — edit it directly here, then publish
  straight from this path. Always pass `url: https://claude.ai/code/artifact/4de5b8d7-5d7c-40d6-ab8c-3e0c75692591`
  to update in place — omitting it forks a duplicate artifact.
- No dedicated publish skill exists for this one (unlike `player-reference-tool`) — just call the
  Artifact tool directly with the `url:` above after editing.
- Linked from the "12 Tails Tools" hub (`12t_projects/player-reference-tool/index.html`) as an
  external link-out entry, not mounted inline — see that project's `CLAUDE.md` for why (2026-08-12
  policy: new hub tools link to their own separate Artifacts rather than being merged into the hub's
  single file).
